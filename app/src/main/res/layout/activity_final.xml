<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".FinalActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="You won!"
        android:id="@+id/win"
        android:textSize="50dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:id="@+id/time_finished"
        android:layout_below="@+id/win"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="150dp"
        android:layout_height="75dp"
        android:text="Play Again"
        android:id="@+id/again"
        android:onClick="onClick"
        android:textSize="25dp"
        android:background="@android:color/transparent"
        android:elegantTextHeight="true"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/time_finished"
        android:layout_toStartOf="@+id/time_finished" />

    <Button
        android:layout_width="150dp"
        android:layout_height="75dp"
        android:text="Share"
        android:id="@+id/share"
        android:textSize="25dp"
        android:onClick="onClick"
        android:background="@android:color/transparent"
        android:layout_alignTop="@+id/again"
        android:layout_alignLeft="@+id/time_finished"
        android:layout_alignStart="@+id/time_finished" />
</RelativeLayout>